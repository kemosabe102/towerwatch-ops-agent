# Normalize line endings: LF in the repository, native in the working copy.
# Authored on Windows; keeps diffs clean for readers on any platform.
* text=auto eol=lf

# Explicitly binary — never normalize.
*.png binary
*.jpg binary
*.gif binary
*.ico binary
*.pdf binary
